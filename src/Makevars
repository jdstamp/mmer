## Use the R_HOME indirection to support installations of multiple R version
PKG_LIBS = `$(R_HOME)/bin/Rscript -e "Rcpp:::LdFlags()"` -lhdf5 -lomp $(CLANGFLAGS) -fopenmp $(LDFLAGS)
PKG_CPPFLAGS = -I../inst/include/ $(CPPFLAGS)
