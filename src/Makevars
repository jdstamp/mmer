## Use the R_HOME indirection to support installations of multiple R version
PKG_LIBS = `$(R_HOME)/bin/Rscript -e "Rcpp:::LdFlags()"` -lhdf5 -lomp $(LDFLAGS) $(SHLIB_OPENMP_CXXFLAGS)
PKG_CPPFLAGS = -I../inst/include/ $(CPPFLAGS)
PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS) -g -O0
